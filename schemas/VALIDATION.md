# Schema Validation

All schemas were checked as JSON Schema Draft 2020-12 documents.
Each schema was also tested against one representative valid fixture.

| Schema | Schema valid | Fixture valid |
|---|---:|---:|
| `observation.schema.yaml` | yes | yes |
| `proxy.schema.yaml` | yes | yes |
| `protocol_candidate.schema.yaml` | yes | yes |
| `simulation_receipt.schema.yaml` | yes | yes |
| `poc_promotion.schema.yaml` | yes | yes |

Validation fixtures are stored in `tests/fixtures/`.
